using System;
using System.Collections.Generic;
using System.Text;

namespace PlanningPoker.Common
{
    public static class AppConstants
    {
        public struct AppConfig
        {
            /// <summary>
            /// The connection name setting 
            /// </summary>
            public static readonly string ConnectionString = "DBConnectionString";
        }
    }
}
